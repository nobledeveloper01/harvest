<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Alerts have to survive a restart.

      A spoilage window is three days and a cheap phone reboots — for an update,
      for a flat battery, because it was switched off overnight to save charge,
      which is ordinary on the design floor. Android drops every scheduled alarm
      on reboot, and `flutter_local_notifications` ships a receiver that puts
      them back; the receiver cannot run without this.

      Nothing declared it, so every warning was one power cycle from being
      forgotten silently. Found beside `exact_alarms_not_permitted`, in the same
      `aapt2 dump permissions` that showed this app asks for INTERNET, VIBRATE
      and POST_NOTIFICATIONS and nothing else. See ADR-0015 and the journal.

      There is deliberately no `SCHEDULE_EXACT_ALARM` here: the alerts are
      inexact, which needs no permission and cannot be refused.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Harvest"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
